import { cn } from "@/lib/utils";
import React from "react";

const AssetTable = ({ className }: { className?: string }) => {
  return <div className={cn("py-10", className)}>AssetTable</div>;
};

export default AssetTable;
